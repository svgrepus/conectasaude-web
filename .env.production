# ConectaSaúde - Configurações do Supabase
# SUBSTITUA pelos valores do seu projeto

# URL do projeto Supabase (encontrar em Settings > API)
SUPABASE_URL=https://[SEU-PROJECT-ID].supabase.co

# Chave anônima/pública (encontrar em Settings > API)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.[SEU-TOKEN-ANON]

# Chave de serviço (encontrar em Settings > API) - MANTER SEGURO!
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.[SEU-TOKEN-SERVICE]

# ID do projeto (extrair da URL)
SUPABASE_PROJECT_REF=[SEU-PROJECT-ID]

# URL de conexão direta com banco (encontrar em Settings > Database)
DATABASE_URL=postgresql://postgres:[SUA-SENHA]@db.[SEU-PROJECT-ID].supabase.co:5432/postgres

# Outras configurações
NODE_ENV=development
PORT=3000

# Configurações de autenticação
JWT_SECRET=[SEU-JWT-SECRET]

# Configurações de e-mail (se usar)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Configurações de armazenamento
STORAGE_BUCKET=municipes-fotos
MAX_FILE_SIZE=5242880  # 5MB em bytes

# URLs dos serviços
POSTGREST_URL=https://[SEU-PROJECT-ID].supabase.co/rest/v1
REALTIME_URL=wss://[SEU-PROJECT-ID].supabase.co/realtime/v1
STORAGE_URL=https://[SEU-PROJECT-ID].supabase.co/storage/v1

# Configurações de segurança
CORS_ORIGIN=http://localhost:3000,http://localhost:8000
ALLOWED_HOSTS=localhost,127.0.0.1,[SEU-PROJECT-ID].supabase.co
